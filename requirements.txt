# Web
fastapi==0.111.0
uvicorn[standard]==0.29.0
gunicorn==22.0.0

# DB
pymongo==4.7.2
python-dotenv==1.0.1

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1            # pin: passlib 1.7.4 se rompe con bcrypt>=4.1/5.x
python-multipart==0.0.9

# Validación (Pydantic v2 — el código usa model_config / pydantic_settings)
pydantic==2.7.1
pydantic-settings==2.2.1
email-validator==2.1.1  # requerido por EmailStr

# Tests
pytest==8.2.0
pytest-cov==5.0.0
httpx==0.27.0
